# Ralph Progress Log
Started: Wed Feb 26 2026
---

## Codebase Patterns
- NestJS apps in this monorepo need separate eslint/prettier configs because the root config uses @trivago/prettier-plugin-sort-imports which doesn't support decorators
- Add NestJS apps to .prettierignore and .eslintrc.js ignorePatterns to avoid decorator parsing errors
- Use `root: true` in app-level .eslintrc.js to prevent inheritance from root config
- NestJS CLI requires tsconfig.build.json in the app directory
- Use dotenv.config() with explicit path for .env.local loading (dotenv/config only loads .env)
- TypeORM CLI scripts need `--project tsconfig.json` flag for ts-node to pick up decorator support
- uuid-ossp extension must be created by superuser (postgres) before running migrations that use uuid_generate_v4()
- PostGraphile uses postgraphile-nest for NestJS integration; use PostGraphileModule.forRootAsync() with factory function
- PostGraphile auto-generates GraphQL API from database; queries use connection pattern: allItems { nodes { ... } }
- Apollo Client uses typed generics: apolloClient.query<ResponseType>({ query }) for proper TypeScript inference
- GraphQL data from PostGraphile converts snake_case columns to camelCase in GraphQL schema
- TypeORM 0.3.x uses DataSource and DataSourceOptions instead of Connection/ConnectionOptions
- TypeORM migration:generate requires -d src/config/typeorm.ts to specify data source

---

## 2026-02-26 - US-004
- Implemented NestJS application bootstrap: main.ts, app.module.ts, app.controller.ts, app.service.ts
- Files created:
  - apps/tictactoe-api/src/app.service.ts - getRoot() returns {message: 'TicTacToe API'}, getHealth() returns {status: 'ok'}
  - apps/tictactoe-api/src/app.controller.ts - GET / and GET /health endpoints
  - apps/tictactoe-api/src/app.module.ts - TypeOrmModule.forRoot + PostGraphileModule.forRootAsync
  - apps/tictactoe-api/src/main.ts - Bootstrap on port 3002 with dotenv loading
- **Learnings for future iterations:**
  - dotenv.config() must be called BEFORE importing AppModule so env vars are available to config modules
  - PostGraphileModule.forRootAsync requires imports/inject/useFactory structure even if imports and inject are empty arrays
  - lint-staged and commitlint hooks run on commit; subject-case warning is non-blocking
---

## 2026-02-26 - US-005
- Implemented database migration and seed data
- Files created:
  - apps/tictactoe-api/src/migrations/1772154510409-CreateGame.ts - Auto-generated migration creating 'game' table with uuid PK, jsonb board_state/moves, varchar status/winner, timestamps
  - apps/tictactoe-api/src/cli/seed.ts - Seeds 2 sample completed games (one x_wins with diagonal win, one draw with full board)
  - apps/tictactoe-api/src/cli/migration.ts - Placeholder migration cleanup utility matching realty-ai-api pattern
- Docker started, uuid-ossp extension created, migration ran, seed data verified in database
- **Learnings for future iterations:**
  - TypeORM migration:generate auto-creates uuid-ossp extension in the migration process
  - JSONB default values in TypeORM entity decorators (like `default: []`) translate to proper SQL defaults in generated migrations
  - Seed script must call dotenv.config() before any imports that depend on env vars (same pattern as main.ts)
  - The `pnpm seed` script uses `ts-node --project tsconfig.json` flag which is already configured in package.json
---
